(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
(async function() {
  for await (const a of new Map) {
  }
});
